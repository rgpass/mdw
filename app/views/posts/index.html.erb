
		
<% if signed_in? %>

<img src="http://tinyurl.com/mhqqyem" class="logopic" /><br />
<% else %>
	<div class="center hero-unit">
		<h1>My Dysfunctional</h1>
		<%= link_to image_tag('http://tinyurl.com/l5vxloq'), root_path %>
		<h1>Workplace</h1>
		<h2>
			<%= link_to "Sign up", signup_path %> or <%= link_to "sign in", signin_path %> to tell the world your horror story.
		</h2>
		<%= link_to "Sign Up!", signup_path, class: "btn btn-large btn-danger" %>
		<h4 class="inline">or</h4>
		<%= link_to "Sign In!", signin_path, class: "btn btn-large btn-danger" %>
	</div>
<% end %>

<div class="row">
	<div class="span8 offset2">
		<div>
			<%= render @posts %>
		</div>
	</div>
</div>
<div class="row-fluid">
	<div class="span12 pagination-centered">
		<%= will_paginate @posts %>
	</div>
</div>

<% if current_user.try(:admin?) %>

<% end %>
