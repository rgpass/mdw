<% provide(:title, "Sign In") %>
<h1>Sign In</h1>


<div class="row">
	<div class="span6 offset3">
		<!-- Since we do not have a Sessions model, thus no -->
		<!-- Sessions class, we cannot use the instance class -->
		<!-- or variable @session (like we use @user). Thus -->
		<!-- we have to define the name of the resource -->
		<!-- and its corresponding URI --> 
		<%= form_for(:session, url: sessions_path) do |f| %>

			<%= f.label :name, "Username" %>
			<%= f.text_field :name %>

			<%= f.label :password %>
			<%= f.password_field :password %>

			<%= f.submit "Sign In", class: "btn btn-large btn-danger" %>
		<% end %>

		<p>New user? <%= link_to "Sign up now!", signup_path %></p>
	</div>
</div>

<% if current_user.try(:admin?) %>
<p>
	Need to add the feature where you can recover your password somehow. Since it's anonymous (no e-mail), need to set up a security question or something.
</p>
	<% end %>